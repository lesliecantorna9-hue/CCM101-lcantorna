## Cloud Infrastructure Components
1. Compute Resources

Compute resources include the CPU and RAM that allow a server to process information and r

In the KillerCoda Linux environment, the CPU and memory shown through commands such as lscpuand free -hare examples of compute resources. These resources allow the Linux server to perform tasks and run programs.

2. Storage

Storage resources are used to save files, applications, operating system data, and other

In the KillerCoda environment, the disk devices and file systems displayed using lsblkand df -hrepresent storage resources. They provide the space needed to store the Linux operating system and other files.

3. Networking Resources

Networking resources allow computers, servers, and users to communicate with each other. They are important in cloud computing because cloud services need network connections to send a

In the KillerCoda environment, the IP address obtained through hostname -Iis an example of a netw

4. Operating System

The KillerCoda environment uses Linux as its operating system. Linux is widely used in cloud computing because it is stable, flexible, secure, and supports many cloud applications and services.

The KillerCoda environment uses Linux as its operating system. Linux is widely used in cloud computing because it is stable, flexible, secure, and supports many cloud applications and services.
